document.getElementById('add').addEventListener('click', function() {
  var value = document.getElementById('item').value; //get user input
  if (value) {

  }
  //console.log(value);
});

//what kind of event and a callback are the arguments that
//addEventListener takes which is the same as .onclick
